from setuptools import setup, find_packages

setup(
    name="farmpower_backend_v2",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Your dependencies will be installed from requirements.txt
    ],
)
