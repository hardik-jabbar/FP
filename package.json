{
  "name": "fp",
  "version": "1.0.0",
  "description": "FarmPower - Modern Farming Solutions",
  "main": "FARMPOWER/server.js",
  "private": true,
  "scripts": {
    "start": "cd FARMPOWER && npm start",
    "dev": "cd FARMPOWER && npm run dev",
    "build": "cd FARMPOWER && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "author": "Hardik Jabbar",
  "license": "ISC",
  "dependencies": {
    "@netlify/functions": "^1.5.0",
    "express": "^4.18.2",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "netlify-cli": "^15.0.0",
    "netlify-lambda": "^2.0.16"
  }
}
